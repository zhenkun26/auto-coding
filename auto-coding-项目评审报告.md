# auto-coding 项目评审报告

- **评审对象**：[zhenkun26/auto-coding](https://github.com/zhenkun26/auto-coding)
- **评审日期**：2026-08-07
- **评审方式**：克隆仓库（本地路径 `/Users/yuzheng/Documents/kimi/workspace/auto-coding`），通读全部核心文件，实际运行测试套件与仓库自检
- **总体评分**：**8 / 10** —— 明显高于同类「AI 编码 skill」仓库的平均水平

---

## 一、项目是什么

**auto-coding** 是一个给 AI 编码助手（Codex 等）使用的「风险感知交付 skill」——不是传统软件库，而是一套 **prompt 工程 + 轻量工具脚本** 的组合：

| 组成 | 内容 |
|:---|:---|
| `SKILL.md`（183 行） | 只保留核心契约、路由规则、工作流和资源地图 |
| `references/*.md`（12 份） | 按需加载的细节文档：路由、规划、实现、验证、风险控制、冲突裁决、断点恢复等 |
| `scripts/`（4 个 Python 脚本 + 1 个 sh） | 只读项目探测、原子化状态管理、AST 契约比对、仓库自检、插件包同步 |
| 工程配套 | pytest 测试套件、GitHub Actions CI、OpenSpec 规格（dogfooding）、双语 README |

**核心理念**：按风险而非改动规模决定执行深度——

- 三条执行路径：**Fast / Standard / High-risk**；即使只改一行代码，涉及鉴权或金额也算 High-risk
- 验证证据严格区分 `PASS / FAIL / BLOCKED / NOT_APPLICABLE`，**不允许把没跑成的验证说成通过**
- 提交、装依赖、部署、删除、迁移等副作用操作**必须显式授权**
- 最小而完整的改动原则：复用 > 标准库 > 已装依赖 > 新代码
- 默认不产生过程文件，长任务仅用单一状态文件

## 二、验证结果（实测）

| 项目 | 结果 |
|:---|:---|
| pytest 测试套件 | ✅ **51 个用例全部通过**（4.69s） |
| `scripts/check_repo.py` 仓库自检 | ✅ 通过（链接 / 许可头 / 双语 README 结构一致性） |
| CI 配置 | ✅ 存在且合理（push/PR 触发，跑测试 + 自检） |
| `sync_plugin_skills.sh` | ✅ 可正常执行 |

## 三、写得好的地方

### 1. Skill 架构设计成熟（最突出的优点）

主文件只放契约、细节按需读取（progressive disclosure），是目前 agent skill 的最佳实践，上下文成本控制良好。资源地图中每份 reference 都标注了「什么时候读」的条件，设计意图清晰。

### 2. 诚实性机制设计对症

LLM 编码助手最常见的毛病就是虚报验证结果。本项目把以下规则写成不可协商条款：

- `BLOCKED` 不能当 `PASS`
- 逃逸门（用 `Any` / `# type: ignore` / `cast()` 蒙混过关的自愈）必须如实列出，记为质量债
- 降级不等于跳过：降级的检查仍需替代证据并明确标注

这些条款精准针对 LLM 编码的真实失败模式。

### 3. 脚本工程质量扎实

- `manage_state.py`：temp file + `os.replace` 原子写入，崩溃不会留下半个 JSON；纯标准库；参数校验与损坏文件报错路径齐全
- `check_python_contracts.py`：用 AST 做结构契约比对，支持 `ClassName.method` 命名空间感知避免同名方法误报；空契约不会假性通过
- `detect_project.py`：只读探测，不执行任何项目工具，输出明确定位为「发现证据而非执行许可」

### 4. 有真实的对抗性自审记录

`docs/ACCEPTANCE_REPORT.md` 记录了 v0.1.0 审查发现的真实缺陷（裸 `except` 吞异常、`sys.argv` 越界、静默跳过解析失败、非 UTF-8 崩溃等）及修复状态，并诚实声明「这不是业务服务，没有金融级风险面」。这种自我批判的文档在同类项目中很少见。

### 5. 工程配套完整

MIT 许可 + THIRD_PARTY 归属（复用梯子改写自 Ponytail）、Keep a Changelog、双语 README、CI 跑测试与仓库自检、用 OpenSpec 管理自己的规格（dogfooding）。

## 四、可以挑剔的地方

### 1. 大量文件重复维护

根目录的 `SKILL.md`、`references/`、`scripts/` 在 `plugins/auto-coding/skills/auto-coding/` 下完整复制了一份，靠 `sync_plugin_skills.sh` 手动同步防漂移。虽有单一事实源约定，但复制终究是隐患——忘记同步就会发布旧版本。更干净的做法是发布时构建生成，而非把副本提交进仓库。

### 2. 核心机制依赖 agent 自律，无法强制

路由分级、逃逸门上报、L0/L1/L2 自检全靠「skill 写了，agent 照做」。没有任何 eval harness 或客观度量证明这套 skill 真的能改善 agent 行为——这是所有 prompt 类项目的通病，但本项目也未尝试缓解。

### 3. 契约检查的正则解析较脆弱

`check_python_contracts.py` 用正则从 spec 提取签名，作者自己承认只覆盖约 80% 结构检查且可能误匹配正文（已文档化为已知限制）。模块级全局 `DIAGNOSTICS` 列表对 CLI 够用但不够干净。

### 4. 启发式规则偏粗

- `detect_project.py`：「存在 `tests/` 目录」被当作 pytest 已配置，偏宽松；源码根目录探测取第一个命中候选即返回，`src` 与 `cmd` 并存时会漏
- `manage_state.py`：列表字段用 `;` 分隔，值中含 `;` 会静默截断

### 5. CI 不检查自身代码质量

CI 跑 pytest 和仓库自检，但没有对自家脚本跑 ruff / mypy——而它给别的项目定的标准恰恰是「类型检查 Critical、lint Standard」。对己要求低于对人要求。

### 6. 项目极新、迭代有反复

全部提交集中在 2026-08-06 ~ 08-07 两天。v0.1.0 做了 Docker 多阶段镜像 + K8s 八件套（对一个文档+脚本仓库明显过度工程），v0.2.0 又全部删除——判断力在向好的方向演进，但也说明前期有「为显得生产级而生产级」的倾向。另外 `.codex/` 中 vendored 的 OpenSpec 技能与提交风格显示项目本身大概率是 AI 辅助生成的——不算缺点，但读者应知情。

## 五、分项评分

| 维度 | 评分 | 说明 |
|:---|:---:|:---|
| 架构设计 | 9/10 | progressive disclosure、按需 references、风险路由，结构克制 |
| 文档质量 | 9/10 | 双语 README、诚实声明、CHANGELOG、自审报告齐全 |
| 代码质量 | 7/10 | 脚本扎实但有小瑕疵；正则解析脆弱；重复维护 |
| 测试与 CI | 7/10 | 51 用例真跑真过，但缺对自身代码的 lint/type 检查 |
| 实际效用 | 6/10 | 机制依赖 agent 自律，无客观效果验证手段 |
| 工程成熟度 | 7/10 | 项目极新、单作者、迭代有反复 |
| **总分** | **8/10** | 同类仓库中明显高于平均 |

## 六、结论

项目的价值不在代码量（约 7k 行总量中真正的代码不到 1.5k 行），而在于**把 vibe coding 的踩坑经验沉淀成了一套结构克制、边界清晰的规则体系**：风险路由、授权边界、诚实验证、零过程文件，每一条都针对 LLM 编码的真实失败模式。文档诚实（包括承认历史版本的过度工程）、脚本能跑、测试真过、有自审记录。主要短板是「效果无法客观验证」这一 prompt 工程的固有天花板，以及文件重复维护、CI 自标准不一致等工程细节。

**值得借鉴的部分**：「主文件只放契约 + 按需 references + BLOCKED 不等于 PASS + 显式授权边界」这套结构，做类似 agent skill 项目时可以直接复用。
